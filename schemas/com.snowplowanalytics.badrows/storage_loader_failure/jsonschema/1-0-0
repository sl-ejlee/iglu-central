{
  "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
  "description": "Schema for a storage loader failure bad row",
  "self": {
    "vendor": "com.snowplowanalytics.badrows",
    "name": "storage_loader_failure",
    "format": "jsonschema",
    "version": "1-0-0"
  },
  "type": "object",
  "properties": {
    "failure": {
      "type": "object",
      "description": "Information regarding the storage loader failure",
      "properties": {
        "schema": {
          "type": "string",
          "description": "Schema of the particular storage loader failure",
          "pattern": "^iglu:[a-zA-Z0-9-_.]+/[a-zA-Z0-9-_]+/[a-zA-Z0-9-_]+/[0-9]+-[0-9]+-[0-9]+$"
        },
        "data": {
          "type": "object",
          "description": "Data associated with the particular storage loader failure"
        }
      },
      "required": [ "schema", "data" ],
      "additionalProperties": false
    },
    "payload": {
      "type": "object",
      "description": "The collector payload that resulted in a storage loader failure",
      "properties": {
        "enrichedEvent": {
          "type": "string",
          "description": "Enriched event associated with this failure"
        },
        "id": {
          "type": "string",
          "description": "Machine-readable unique identifier of the storage loader that produced this failure",
          "format": "uuid"
        }
      },
      "required": [ "enrichedEvent", "id" ],
      "additionalProperties": false
    },
    "processor": {
      "type": "object",
      "description": "Information about the piece of software responsible for the creation of this storage loader failure",
      "properties": {
        "artifact": {
          "type": "string",
          "description": "Artifact responsible for the creation of this storage loader failure",
          "maxLength": 512
        },
        "version": {
          "type": "string",
          "description": "Version of the artifact responsible for the creation of this storage loader failure",
          "pattern": "^(\\d+\\.\\d+\\.\\d+.*)$",
          "maxLength": 32
        },
        "platform": {
          "type": "string",
          "description": "Platform on which the artifact was running when the storage loader failure occurred",
          "maxLength": 64
        }
      },
      "required": [ "artifact", "version", "platform" ],
      "additionalProperties": false
    }
  },
  "required": [ "failure", "payload", "processor" ],
  "additionalProperties": false
}
